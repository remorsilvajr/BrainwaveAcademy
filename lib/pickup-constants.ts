// Kept apart from lib/pickup-list.ts (which imports the service-role client) so
// a Client Component can import it without pulling server-only code into the bundle.
export const ALL_STUDENTS = 'all'
